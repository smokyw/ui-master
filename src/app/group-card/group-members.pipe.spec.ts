import { GroupMembersPipe } from './group-members.pipe';

describe('GroupMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupMembersPipe();
    expect(pipe).toBeTruthy();
  });
});
